# Security

Verify company membership and permission server-side for every operation. Enforce tenant scope in queries and storage. Encrypt sensitive integration credentials; never return secrets to normal API responses or logs. Audit changes, postings, reversals, configuration, permission, authentication, and integration events. Validate uploaded content independently of filename and declared MIME. Security tests must cover cross-tenant denial.
