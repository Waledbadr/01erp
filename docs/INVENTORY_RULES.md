# Inventory rules

Every movement has a source document. Quantities are converted to item-specific base units before valuation. Weighted average cost updates under a row lock on the stock balance. Negative stock is blocked by default. Inventory value and COGS reconcile with ledger postings. See skills/inventory-engine/SKILL.md.
